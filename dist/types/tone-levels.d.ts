/** Shared visual quantization policy, not a geometric accuracy tolerance. */
export declare const TONE_LEVELS = 16;
export declare function validateShadingLevels(value: unknown): asserts value is import('./types.js').ShadingLevels;
