# 排线与非重复点刻：共享表面区域

## 真正移除的重复工作

- 可见性仍由解析边界排列一次求解。`DotRegions.surface(id)` 从同一份经过认证的闭合边界取得可见多边形，保留孔洞与不连通分量；约 0.0015 SVG 单位弦高细分，不是全画幅像素扫描。
- 每个球面排线族一次建立高度索引：把边界段在前半球上的高度范围放入桶。每条排线只访问可能相交的段，在投影椭圆坐标中解二次方程；不再逐线对场景求遮挡交线。索引同时适用于该球面的阴影区域。
- `createSurfaceAtlas` 每个相关表面只提取一次局部二值阴影轮廓。后续排线与点刻只查询轮廓，不再每个纹理采样点发射阴影射线。平行光明暗阈值在有／无阴影区域分别反解，然后合并有效区间。
- 排线沿原曲面方向，平滑区域自适应求线宽。硬阴影交界保留双侧宽度，不把交界误当成收尖；可见性／明暗阈值造成的实际线尾仍收尖。
- 点刻对整个候选格加最大圆点包围范围做一次归属／边界距离认证；通过认证的格不再逐点查归属、深度和边界距离。边界格仍使用原逐点缩径规则。
- 相同半径的圆点合并为 `stroke-linecap="round"` 的零长度 `M…h0` 子路径。圆点位置并未重复或量化成固定图案，仍使用原随机流。去掉的是每个圆点重复的 XML 属性，不是圆点本身。低层 `buildDots` 未请求 `compactStipple` 时仍输出独立 circle。

## 测量

`benchmarks/regions-paired.json`：同一 Node 进程加载修改前保存的完整 ESM 与新引擎，各预热两次；每个配置交替顺序测量五对，分别取中位数。固定 scale 60、粗细 1、预览、元素底色开启，其余默认。只测 SVG 字符串生成，不含浏览器解析／绘制，也不是刷新速度保证。

| 模型 | 阴影 | 排线 前→后 ms | 点刻 前→后 ms |
| --- | --- | ---: | ---: |
| 单球 | 关 | 7.74 → 7.30 | 10.44 → 7.32 |
| 单球 | 开 | 6.28 → 5.21 | 10.57 → 7.97 |
| 乙醇 | 关 | 45.43 → 33.18 | 35.87 → 29.88 |
| 乙醇 | 开 | 35.43 → 39.88 | 35.57 → 35.33 |
| C60 | 关 | 476.20 → 354.64 | 653.18 → 507.80 |
| C60 | 开 | 759.54 → 600.65 | 833.88 → 718.72 |

C60 排线在本轮成对测量中耗时降低约 **21–26%**，点刻约 **14–22%**。小场景阴影区准备成本可能抵消收益：乙醇阴影排线此次反而较慢。不能把这些结果说成所有模型都加速。

C60 点刻 SVG：无阴影 **7,321,682 → 3,061,462** 字节；有阴影 **11,363,939 → 4,674,844** 字节，分别减少约 **58%／59%**。排线文件没有一起变小：阴影排线 **3,724,903 → 4,651,374** 字节。精确区间端点与双侧宽度的表达仍有输出成本；当前不是通用的 SVG 压缩器。

`regions-before.json` 是修改前单独运行的记录；`regions-intermediate.json` 是第一次接入共享区、尚未加入高度索引与点刻子路径合并的中间记录。最终结果以 `regions-paired.json` 为准。不同运行中的 JIT、GC 和进程条件会影响绝对毫秒数，不应与上一轮历史表直接相除。

重测：

```sh
# 必须在改代码前执行，否则会覆盖旧基线：
node scripts/bench-shared-regions.mjs --capture
npm run build
node scripts/bench-shared-regions.mjs --paired
```

基线模块保存在忽略跟踪的 `build/baselines/pre-regions.mjs`。缺少它时不能进行前后成对比较。

## 验证与误差

- `test-region-clipping.cjs`：11 组独立数值检查，覆盖孔洞、岛、相切、参数接缝、共线、病态投影、不同倾斜方向的前半球高度索引及回退。
- `test-shared-regions.cjs`：对单球／乙醇／C60、有／无阴影直接禁用旧逐线场景求交方法，确认正常路径仍可完成；重复纹理查询期间阴影采样计数及物理遮挡回调次数不增加。
- 同测试逐个比较无阴影三种模型的所有圆点：序列化后的坐标、半径和重数完全一致，只是 SVG 编码顺序／结构改变。并非以几个平均值代替点位验证。
- 乙醇 11,330 个可见球面探针，局部阴影判定与物理参考不同的点为平行光 10 个（约 0.088%）、点光源 16 个（约 0.141%）。这是该测试配置的结果，不是任意场景的误差上界。
- 同一固定窗口的阴影排线黑面积比例：乙醇 0.01624246 → 0.01623311；C60 0.19056357 → 0.19086657，C60 差约 0.0303 个百分点。窗口含背景，不能当成表面本身的平均覆盖率。
- 既有物理光照、排线曲线／明暗、三种纹理覆盖率及规则网点结构测试继续通过。

### 明确限制

1. 阴影发现网格沿用每表面预览 2／导出 1 SVG 单位，并细化已发现的边界；大型区域有采样数量上限。二分不能保证发现比网格细的独立阴影，复杂拓扑或薄阴影仍可能遗漏。
2. 高度索引只用于可认证的球面投影；病态图表、无法认证的可见区域和低层自定义上下文保留安全回退。点光源仍需沿线采样变化的明暗，但不重复查询阴影射线。
3. 有阴影点刻会因局部轮廓近似及全局平均黑度校准而增减少量点，不能声称有阴影输出逐点相同。
4. SVG 圆头零长度子路径是矢量圆盘；逐点独立编辑需先拆分子路径。批量路径与独立 circle 的边缘抗锯齿合成可能有查看器相关的微小差异。本轮没有浏览器绘制计时或截图验收。
5. 仍需生成每条排线的几何和每个非重复圆点的位置。已移除重复区域求解，不代表剩余工作不再随纹理数量增长。
