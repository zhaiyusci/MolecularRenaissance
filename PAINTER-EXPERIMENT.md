# 前景覆盖实验

基线已先提交为 `b79d28d`，未推送。实验为独立入口 `src/painter-experiment.ts`，未替换默认渲染器，也未提交实验改动。未改动无关的 apparatus-engraving 实验。

本文数字与实验观察保留为历史记录，未在本次移除点光源后重测。当前仅支持平行光；`lightType`、`lightDistance`、`lightAttenuation` 显式提供会报错 `Removed lighting option: ${key}; only directional lighting is supported`，不再回退支持点光源。

## 实测结论

同一编译模块图、相同参数，各预热两次，轮换执行五组三种策略并取中位数。只计 SVG 生成，不计浏览器绘制。1000×1000、scale 60、yaw/pitch 0、标签 24、排线、平行光、无投影阴影、元素底色开启。原始数据：`benchmarks/painter-experiment.json`。

| 场景 | 原版 ms | 仅前景覆盖 ms | 覆盖＋排线复用 ms |
|---|---:|---:|---:|
| 单球 | 6.711 | 4.810 | 4.037 |
| 两个前后分离、投影重叠的球 | 13.468 | 11.489 | 5.865 |
| 两个不同半径的嵌套球 | 41.972 | 18.288 | 18.064 |
| 64 个互不相交的球 | 501.951 | 437.621 | 7.426 |
| 前后两层共 32 个球 | 130.266 | 226.890 | 7.781 |

**显著收益主要来自完整排线复用，不能全部归因于省掉遮挡计算。** 平行光且无投影阴影时，同半径球的排线只差平移：每次渲染生成一次完整矢量模板，通过 SVG use 复用，不是跨帧缓存，也不是位图。64 球输出从 8,371,762 降到 170,439 字节。没有复用时，生成被覆盖的纹理会抵消甚至超过省下的可见性计算：32 球堆叠案例就是反例。浏览器仍需绘制 use 实例，不能将核心时间直接当成屏幕刷新时间。

## C60 尚未获得收益

- 原乙醇与原 C60 含键，此首轮球体原型明确回退原引擎。
- 明确移除键的 C60 夹具仍遇到 `front-surfaces-swap`：相交球的前半球在交叠区发生前后关系反转，不能用一个整球顺序代替。
- 原 C60 本轮原版 229.778 ms，实验回退 241.012 ms，SVG 相同。这不是 C60 加速方案。
- 本实验未实现交叉区域的局部拆分。下一步如要用于真实球棍模型，必须处理这部分及球—键接缝，不能靠忽略键、减少半径或任意中心排序冒充正确加速。

## 认证与限制

投影不相交的球无依赖；包含关系确定内球先画；三维不相交的球，其前表面深度差在连通投影交集内不换号，可由一个内部点确定先后。三维相交时，解析求交圆最高 Z，检查是否进入双方前半球；可能换序、相切数值不确定、依赖成环均回退。拓扑排序不是按中心 Z 猜测。

安全路径不构建可见边界排列、不逐点查询场景可见性，也不以标签中心是否可见决定整字显示。完整球填色、排线、轮廓、文字随层绘制。只为粗排线使用所属球的完整轮廓裁切，文字及祖先容器没有裁剪。所有球均保留，尚未做完全隐藏物体剔除。

当前快路径仅支持无键、平行光、无投影阴影的排线场景。投影阴影、其他纹理模式及无法认证的球体关系均调用原引擎，保留原输入与输出；已删除的光照选项直接拒绝。最大规划预算 2048 球，规划仍为二次复杂度。

`test-painter-experiment.mjs`：406 组明确／随机场景中 362 组通过认证、44 组保守回退；对通过场景执行 495,578 个独立最近前表面探针，与最终覆盖归属一致。另检查嵌套球不可按中心排序、隐藏标签中心仍生成整字、无文字裁剪、模板复用前后排线几何相同、确定性及回退 SVG 完全一致。没有浏览器截图验收。

## 使用

```sh
npm run build
node test-painter-experiment.mjs
node scripts/bench-painter.mjs
node scripts/demo-painter.mjs
```

离线对照页：`experiments/painter/index.html`。保留前后 SVG，可人工检查。默认应用与生产 ESM 不暴露实验入口；Node 原型可通过 `scripts/load-painter.mjs` 的 `load('painter-experiment.js')` 加载。`renderPainter` 返回含 `svg`、`route`、`reason`、排序与工作计数的对象；第三参数 `{reuseHatches:false}` 用于关闭模板复用的消融对照。
