# 规则网点：从全画幅扫描改为表面几何

本次本地测量使用同一脚本、模型与参数：平行光、`textureScale: 1`、`quality: 'preview'`、元素底色开启，其余默认。每项预热一次，取随后三次的中位数。**只计生成 SVG 字符串的时间，不包含浏览器解析、绘制或屏幕刷新。** 数字会随机器、JIT、模型与视角变化，不是运行时间保证。

| 模型 | 投影阴影 | 旧网点 ms | 新网点 ms | 同轮排线 ms |
| --- | --- | ---: | ---: | ---: |
| 单球 | 关 | 16.11 | 1.84 | 6.78 |
| 单球 | 开 | 12.69 | 1.92 | 5.42 |
| 乙醇 | 关 | 53.33 | 15.65 | 42.73 |
| 乙醇 | 开 | 59.98 | 16.61 | 30.03 |
| C₆₀ | 关 | 521.11 | 72.51 | 262.08 |
| C₆₀ | 开 | 766.19 | 131.66 | 452.36 |

C₆₀ 的网点生成约加速 **7.2×（无阴影）／5.8×（有阴影）**。该轮输出 SVG 从约 2.23 MB／2.17 MB 降到 0.82 MB／1.02 MB。原始测量保存在 `benchmarks/halftone-before.json`、`benchmarks/halftone-after.json`。

## 实现变化

- 平行光不再构造密集的全画幅明暗网格、再扫描 16 遍求分档轮廓。直接求球面截圆、圆柱角区间及可见端盖，输出短曲线。
- 复用解析可见区域，保留每个表面身份、遮挡、孔洞和不连通分量；网点路径不再建立逐圆点可见性查询索引。
- 阴影只在有潜在遮挡者的表面局部采样一个二值区域，边缘二分细化；各档阴影明暗仍使用几何曲线。候选过滤是保守的，不随意忽略远处投影阴影。
- 点光源与无法认证的几何采用逐表面的局部数值处理。采样步长与网点间距无关，不以圆点数量决定计算量；不承诺复杂场景也能达到表格中的速度。
- 仍输出纯矢量、同相位的 45° pattern；保留平滑光照、整体平均覆盖率校准、黑色纹理、元素底色和独立轮廓。
- 每层绘制范围限定在当前表面的包围盒，避免让浏览器对每个表面都处理整幅画面的填充矩形。

## 有针对性的检查

```sh
npm run build
node test-surface-tones.cjs
node test-surface-shadows.cjs
node test-pattern-halftone.cjs
node test-style-coverage.cjs
node scripts/bench-halftone.mjs
```

检查包括曲线内外判定与独立表面法线的比较、局部阴影候选与全场景射线结果比较、细网点 0.2、图案相位、纯矢量结构及平均黑色覆盖率。不启动浏览器、不使用截图。局部采样仍可能漏掉亚采样尺度的薄阴影或细小拓扑；16 档明暗也仍有量化误差。
