# 纹理性能记录

**最新：** [共享区域重构与成对测量](SHARED-REGIONS.md)。以下保留前几轮的历史结果，不应与新一轮绝对毫秒数直接相除。当前排线已复用按高度索引的可见边界与阴影区域，点刻已按候选格认证并合并圆点子路径；不再是文末描述的初步局部优化。

当前仅支持平行光；`lightType`、`lightDistance`、`lightAttenuation` 已删除，显式提供会报错 `Removed lighting option: ${key}; only directional lighting is supported`。下文点光源相关实现、测试计数和观测均为移除前的历史记录；数字未重新测量。

## 规则网点：从全画幅扫描改为表面几何

本次本地测量使用同一脚本、模型与参数：平行光、`textureScale: 1`、`quality: 'preview'`、元素底色开启，其余默认。每项预热一次，取随后三次的中位数。**只计生成 SVG 字符串的时间，不包含浏览器解析、绘制或屏幕刷新。** 数字会随机器、JIT、模型与视角变化，不是运行时间保证。

| 模型 | 投影阴影 | 旧网点 ms | 新网点 ms | 同轮排线 ms |
| --- | --- | ---: | ---: | ---: |
| 单球 | 关 | 16.11 | 1.84 | 6.78 |
| 单球 | 开 | 12.69 | 1.92 | 5.42 |
| 乙醇 | 关 | 53.33 | 15.65 | 42.73 |
| 乙醇 | 开 | 59.98 | 16.61 | 30.03 |
| C₆₀ | 关 | 521.11 | 72.51 | 262.08 |
| C₆₀ | 开 | 766.19 | 131.66 | 452.36 |

C₆₀ 的网点生成约加速 **7.2×（无阴影）／5.8×（有阴影）**。该轮输出 SVG 从约 2.23 MB／2.17 MB 降到 0.82 MB／1.02 MB。原始测量保存在 `benchmarks/halftone-before.json`、`benchmarks/halftone-after.json`。

## 实现变化

- 平行光不再构造密集的全画幅明暗网格、再扫描 16 遍求分档轮廓。直接求球面截圆、圆柱角区间及可见端盖，输出短曲线。
- 复用解析可见区域，保留每个表面身份、遮挡、孔洞和不连通分量；网点路径不再建立逐圆点可见性查询索引。
- 阴影只在有潜在遮挡者的表面局部采样一个二值区域，边缘二分细化；各档阴影明暗仍使用几何曲线。候选过滤是保守的，不随意忽略远处投影阴影。
- 点光源与无法认证的几何采用逐表面的局部数值处理。采样步长与网点间距无关，不以圆点数量决定计算量；不承诺复杂场景也能达到表格中的速度。
- 仍输出纯矢量、同相位的 45° pattern；保留平滑光照、整体平均覆盖率校准、黑色纹理、元素底色和独立轮廓。
- 每层绘制范围限定在当前表面的包围盒，避免让浏览器对每个表面都处理整幅画面的填充矩形。

## 有针对性的检查

```sh
npm run build
node test-surface-tones.cjs
node test-surface-shadows.cjs
node test-pattern-halftone.cjs
node test-style-coverage.cjs
node scripts/bench-halftone.mjs
```

检查包括曲线内外判定与独立表面法线的比较、局部阴影候选与全场景射线结果比较、细网点 0.2、图案相位、纯矢量结构及平均黑色覆盖率。不启动浏览器、不使用截图。局部采样仍可能漏掉亚采样尺度的薄阴影或细小拓扑；16 档明暗也仍有量化误差。

## 排线与非重复点刻：保留风格的优化

这是后续的一轮独立测量，不应将其中的绝对耗时与上表混算。脚本 `scripts/bench-shading.mjs` 使用相同的预览、底色、粗细 1 和三次中位数设置；仍然**不含浏览器绘制**。原始数据见 `benchmarks/shading-before.json` 与 `benchmarks/shading-after.json`。

| 模型 | 阴影 | 排线：前 → 后 ms | 点刻：前 → 后 ms |
| --- | --- | ---: | ---: |
| 单球 | 关 | 6.24 → 4.53 | 9.62 → 8.14 |
| 单球 | 开 | 6.30 → 5.06 | 8.60 → 9.34 |
| 乙醇 | 关 | 35.85 → 36.43 | 37.64 → 39.66 |
| 乙醇 | 开 | 31.97 → 34.53 | 35.79 → 37.31 |
| C₆₀ | 关 | 284.26 → 276.01 | 339.37 → 294.04 |
| C₆₀ | 开 | 891.47 → 673.60 | 784.52 → 583.08 |

收益不像规则网点那样普遍或显著：C₆₀ 有阴影时两种模式约减少四分之一的生成时间；无阴影排线变化很小；小模型有些项目反而稍慢，不能声称全面提速。C₆₀ 排线 SVG 无阴影约 **5.75 → 3.88 MB**、有阴影约 **6.79 → 3.72 MB**；点刻保留独立圆点，文件体量基本不变。

### 排线

- `src/hatch-curves.ts` 解析求解平行光下的明暗阈值区间，和已有可见区间相交。阈值反解考虑亮度与对比度，不改变球面排线方向或屏幕密度。
- 无潜在投影阴影的已认证区域，用几何与线宽误差驱动采样；等宽中心线直接输出短贝塞尔圆弧或直线。渐变线宽仍连续，端部保留 smoothstep 收尖，并补充收尖区采样。
- 存在潜在阴影、点光源或未知可见拓扑时，**保留原来的沿线发现网格**，先确定断线位置与光照，再精简输出点；没有用稀疏采样冒充精确阴影，也没有将曲面排线换成平铺直线。
- 保守筛选后的光照按表面复用；不需要光照的轮廓不再浪费阴影射线检查。沿线阴影结果尚未改为共享的二维阴影遮罩。

### 点刻

复用相同的表面光照与保守阴影候选，不改变 Poisson 随机流、点位、点径、边界缩点和平均覆盖率算法。当时点光源保留全遮挡者列表，避免不安全的筛选。默认模式仍然是非重复点刻，不是 pattern。

### 验证和复现

```sh
npm run build
node test-hatch-optimization.cjs
node test-surface-lighting.cjs
node test-style-coverage.cjs
node scripts/bench-shading.mjs
```

- 33,200 次解析明暗区间判定、投影椭圆短弧及不同光照下的有限性检查。
- 52,560 个表面采样，与独立物理光照参考及通用回调精确比较；另有 4 组底层点刻 SVG 一致性检查。
- 本地保存的旧引擎对比中，单球排线平均覆盖率从 19.063% 到 19.126%，约差 **0.063 个百分点**；球、乙醇、C₆₀ 的默认点刻圆点分组逐字节一致。数值检查不是浏览器视觉等价证明。
- `--capture` 会复制当前引擎到被忽略的 `build/baselines/pre-shading.mjs` 并记录前测，应在修改引擎之前使用。该备份不随仓库分发；存在时排线测试会额外进行旧版对比，缺失时会跳过该部分。

独立随机 pattern 点刻原型见 [STIPPLE-PATTERN-PROTOTYPE.md](STIPPLE-PATTERN-PROTOTYPE.md)。它展示周期重复、裁断圆点和分档误差，未接入正式模式，也不宣称与原点刻等价。
