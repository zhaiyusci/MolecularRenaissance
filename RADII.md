# 原子半径、单位与固定比例

## 数据来源与定义

采用 **Cordero 等人的经验共价半径**，不是范德华半径，也不是原子电子云的硬边界：

> B. Cordero et al., *Covalent radii revisited*, Dalton Trans. (2008), 2832–2838. [DOI: 10.1039/B801115J](https://doi.org/10.1039/B801115J)

以下数值与 [ASE 的实际半径表及文献注释](https://gitlab.com/ase/ase/-/raw/master/ase/data/__init__.py)、[python-periodictable 的带出处转录](https://github.com/python-periodictable/periodictable/blob/master/periodictable/covalent_radius.py)核对。核验依据是这两份科学软件数据表，并非声称直接查阅了出版商 PDF。

| 元素 | 共价半径 / pm | 本项目使用 / Å | 表列不确定度 / pm |
| --- | ---: | ---: | ---: |
| H | 31 | 0.31 | 5 |
| C（sp³ 参考） | 76 | 0.76 | 1 |
| N | 71 | 0.71 | 1 |
| O | 66 | 0.66 | 2 |
| P | 107 | 1.07 | 3 |
| S | 105 | 1.05 | 3 |

**1 Å = 100 pm。** 当前按元素选择一个默认值，碳遵循 ASE / periodictable 的 sp³ 参考值约定；不会从只有原子索引的键列表猜测杂化态。文献另列 C(sp²) = 0.73 Å、C(sp) = 0.69 Å。需要指定化学环境时，调用方可以明确提供 `atom.radius`：

```ts
const atom = { element: 'C', position: [0, 0, 0] as const, radius: 0.73 };
```

为支持 XYZ 导入，半径表现已扩展到 **H–Cm（原子序数 1–96）**，使用上述 ASE 数据表中实际列出的 Cordero 值；原先六种元素数值不变。ASE 对 Bk–Og 的 `missing = 2.0` 占位值未当作真实半径收录。`test-xyz.cjs` 另检查 F、Cl、Fe 与跨模块 XYZ 解析。

未收录的元素不再偷偷使用碳半径：必须显式提供正、有限的 `atom.radius`，否则报错。数据实现位于 `src/radii.ts`，公开导出 `covalentRadii` 和 `covalentRadiusSource`。

## 不再自动 rescale

输入原子坐标和半径均约定为 **Å**。投影只按显式比例变换：

```text
显示球半径（Å）= 基准半径（atom.radius 或文献值）× atomRadiusScale
投影半径（SVG 单位）= 显示球半径（Å）× scale（SVG 单位/Å）
```

**两个倍率互相独立，均由用户控制。** `atomRadiusScale` 是无量纲的原子球显示倍率，默认 **0.75**，GUI 范围 0.2–2；例如设为 0.5，仅将球半径减半，不改变科学半径表、原子中心、中心间距离、键长或键柱半径。显式 `atom.radius` 也会乘这个倍率。不根据相交、分子、视角或画幅自动挑选倍率。

`scale` 是整个场景的投影比例，默认固定为 **60**。它不随分子大小、视角、元素种类或输出画布尺寸变化，没有 auto-fit 分支；保留平移居中，不缩放模型坐标。

```ts
render(model, { atomRadiusScale: 0.5, scale: 60, width: 900, height: 700 });
```

例如 C 的文献基准为 0.76 Å。在 scale=60 时，atomRadiusScale=1 的投影半径是 45.6 SVG 单位；用户设为 0.5 后是 22.8 SVG 单位。单个原子和整个分子中的同类原子使用相同比例。调大画布不会放大模型，旋转也不会改变比例。

GUI 提供手动比例滑块（20–200，默认/重置 60）。SVG 按其固有画幅显示，较小的窗口使用滚动条，不用 CSS 把整张 SVG 自动缩放到容器。模型超过 SVG 画幅时可能被裁切，需要用户明确减小比例或增大画幅；不会暗中缩小原子。

## 相交球体与键

共价半径并非排斥体积。给定键长未必恰好等于两个表列半径之和。例如理想化 C60 模型仍保持 1.42 Å 等键长，采用默认 C=0.76 Å 时两个相邻球会相交；不会为消除相交自动修改坐标、半径或键长；用户可自行降低 `atomRadiusScale` 来选择球棍图的球大小。

解析路径现支持普通的部分球球相交，使用真实三维球面交圆的投影分割可见区域。完全埋在两端球体并集内的键柱仍保留在物理场景中，只省略已证明不可见的几何边界和排线。包容、近相切、病态投影等未可靠支持的情况仍回退，不猜测连接。

键柱半径 0.115 Å 仍只是绘图参数，**不是有物理定义的“化学键半径”**。内置构型为示意/理想化坐标，未经能量优化；采用文献半径不等于这些构型变成了实验结构。

## 验证

- `test-radii-scale.cjs`：六元素数据、显式半径、未知元素错误、模型/视角/画布不改变比例；三种模块格式的实际 SVG 圆半径。
- `test-sphere-intersections.cjs`：独立射线验证相交球的并集、颜色、原始表面身份、轮廓、排线以及被球体包住的键；保留近轴退化防护。
- 原 TypeScript 迁移的 49 组 SVG 哈希不被覆盖：测试通过明确的历史半径及固定比例夹具保留数值算法基线，不再依赖旧默认值，也不向生产渲染器加回自动缩放。
