# 移除点光源

渲染器现在只使用相机坐标系中的方向光。保留方位角、仰角、投影阴影及阴影强度；快速模式仍不支持投影阴影。

## 已删除

- 点光源位置、随表面位置变化的入射方向、距离衰减计算。
- 点光源专用的有限射线距离；阴影求交现在沿无限方向光射线进行。
- `LightType` 类型及 `lightType`、`lightDistance`、`lightAttenuation` 三个渲染选项。
- 页面中的点光源开关、距离和衰减控件、对应事件与翻译。
- 只服务于点光源的测试；方向光测试、阴影判据与几何容差保留，并增加已删除选项的拒绝测试。

## 调用迁移

删除上述三个旧选项，包括 `lightType: 'directional'`。不要把键留着并赋值为 `undefined`；运行时会明确拒绝任何显式提供的旧键，避免把点光源请求静默解释成方向光。

```js
render(molecule, {
  lightAzimuth: -0.5,
  lightElevation: 0.6,
  castShadows: true,
  shadowStrength: 0.8
});
```

`shadowStrength` 调整遮挡造成的亮度损失，不是光源距离衰减。光影亮度、对比度和渐变线宽也继续保留。快速／精确模式切换只保存恢复阴影偏好，不再保存不存在的光源类型。

## 仍存在的数值路径

删除点光源不等于所有渲染都已解析化：方向光的局部阴影区域仍有数值发现网格；不支持或无法认证的几何仍有保守回退；低层自定义光照回调也可能需要采样。这些未在本次被删除或改成不可靠的解析结果。

## 输出兼容性

快速模式的SVG ID由规范化选项参与哈希。删除三个默认字段会改变该命名空间，这是预期的结构变化，不应伪称完整字符串仍相同，也不为旧哈希重新塞回死选项。对照仅允许统一替换一个经验证的 `fast-painter-<hash>-<hash>` 前缀，其余内容严格比较；精确模式不做此替换。

历史性能报告和原始数据保持原样，不用新实现覆盖旧测量结论。当前可执行基准／对照脚本已去掉旧选项，点光源测试矩阵改为方向光角度矩阵；它们不再代表原来的点光源覆盖。

## 验证结果

- 完整 `npm test`：50个CJS脚本全部通过，包含旋转压力测试；严格类型检查及差异检查通过。
- 默认测试入口之外的3个MJS测试也通过：快速渲染、实验绘制顺序、72组白色键组合。
- 删除前后284组方向光对照：182组完整SVG逐字节相同；102组快速模式仅上述命名空间不同。
- 原生QuickJS：5组完整SVG相同、1组快速模式仅命名空间不同，三个旧选项均按约定拒绝。
- 测试迁移修正：新方向光用例显式映射到旧夹具的物理比例；额外快速模式测试不再依赖可缺失、未固定版本的本地快照，改用已校验Git哈希的历史无标签对照，完整标签约束另行保留。没有放宽几何容差。
