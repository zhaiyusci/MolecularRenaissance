# 边界模块的数值参数

本次只整理 `src/boundaries.ts` 的命名与说明，不调参、不删除回退、不改变计算顺序。数值定义以源码为准，不新增用户配置接口。

## 分清三种用途

| 类别 | 例子 | 含义与调整风险 |
|---|---|---|
| 工作预算／性能选择 | `MAX_SCENE_PRIMITIVES`、`CLEARANCE_INDEX_MIN_CURVES`、`CLEARANCE_INDEX_LEAF_CAPACITY` | 分别控制解析路径的规模上限、是否建树、叶节点容量。64和8是现有实现的启发式选择，不是最优性的结论；规模上限还会影响是否回退。 |
| 几何与数值判定 | `VERTEX_SNAP_SVG`、`SPHERE_TANGENCY_BAND_ANGSTROM`、`ROOT_RESIDUAL_TOLERANCE` | 分别涉及顶点合并、球体相切判断、归一化多项式残差。修改可能影响拓扑、求交或支持范围。 |
| 近似／安全余量策略 | `SIDE_PROBE_*`、`CUBIC_TARGET_ERROR_SVG` | 控制探测偏移或路径近似目标。名称表达用途，不等于已经证明的全局误差保证。 |

## 单位不是同一种

- `_ANGSTROM`：项目世界坐标中的长度；`_ANGSTROM2`：长度平方。
- `_SVG`：投影后的SVG用户坐标单位，不直接等于设备像素；`_SVG2`：该单位的平方。
- `_FRACTION`、`_FACTOR`、相对方向检查：无量纲比例。
- `_RADIANS`：弧度。
- 原始曲线参数：椭圆使用弧度，直线使用 `[0,1]`；输出区间另有归一化 `[0,1]` 合并容差，不能直接混用。
- 多项式先按最大系数归一化；根变量可能是直线比例，也可能是半角正切坐标。系数阈值、残差阈值与根区间扩展不是一回事。

## 侧向探测的几个数字

| 名称 | 保留值 | 作用 |
|---|---:|---|
| `VERTEX_SNAP_SVG` | 0.00002 | 判断是否复用已有投影顶点，不是侧向探测的步长 |
| `SIDE_PROBE_CAP_SVG` | 0.0005 | 探测偏移的绝对上限 |
| `SIDE_PROBE_ENDPOINT_CHORD_FRACTION` | 0.01 | 按当前段两端的弦长限制偏移 |
| `SIDE_PROBE_THICKNESS_FRACTION` | 0.05 | 按椭圆厚度估计限制偏移 |
| `SIDE_PROBE_NORMAL_CHORD_FRACTION` | 0.2 | 按本椭圆另一侧的法向弦长限制偏移 |
| `SIDE_PROBE_CLEARANCE_FRACTION` | 0.2 | 按其他曲线的距离估计进一步限制偏移 |

这些限制通过原有的 `Math.min` 逐步收紧，原来的顺序不变。空间索引仍只预选候选；实际判据仍使用当前偏移值。异常数值的处理也未改变。

两个0.2分别约束本椭圆和其他曲线；多个1e-8也分别服务于相切、嵌套、包容、意外接触等判断。**不因为值相同就合并成一个全局EPS。** 相切判断与隐藏接触区域半径估计确实依赖同一个相切带宽，源码保留这种关联。

## 依据与边界

保留并说明已有数学公式的作用。对没有找到独立调优记录或严格推导的阈值，明确标为沿用的经验／实现选择，不补造依据。数学公式中的0、1、2、数组下标、四象限和常见角度分割继续直接书写，避免把正常数学表达式也包装成常量。

若今后调整这些值，应分别验证：支持／回退范围、相切与退化情况、顶点拓扑、标签和遮挡、完整SVG输出以及性能。纯命名整理不能替代这一步。

## 本次验证

- 展开63个新增具名常量、去除注释并规范化语法后，与修改前的完整模块一致。
- 272组SVG与本次整理前的快照逐字节相同。
- 完整 `npm test` 的50个脚本全部通过，未跳过旋转压力测试。
- 本次未重跑性能基准，也未更新历史性能结论。
