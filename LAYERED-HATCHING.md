# 16级曲面排线

## 默认行为与连续模式

16级曲面排线已从可选实验提升为精确模式的默认排线。当前共享控件名为 **16级明暗**（英文 **16-level shading**；历史名称为16级排线），同时控制精确排线和规则网点，默认勾选，重置恢复勾选；也可显式指定：

```js
render(molecule, {
  renderMode: 'precise',
  shadingMode: 'hatch',
  quantizeShading: true,
  variableWidth: true
});
```

精确模式下，`quantizeShading` 省略或为 `undefined` 时默认 `true`。取消勾选或传入 `false`，排线恢复连续变宽、端部收尖算法；规则网点改为45°规则网格上的逐点连续半径圆点，边缘收缩以容纳完整圆点，不承诺与分档 pattern 逐像素一致。开启时排线和规则网点都使用16级，点刻则忽略量化开关。

界面在光影关闭、点刻或快速模式时禁用开关，但保留勾选和未勾选两种偏好；精确规则网点时仍可切换。界面只在精确排线／规则网点时发送 `quantizeShading`，快速／点刻省略，并且不再发送 `hatchMode`。快速模式显式提供 `quantizeShading: true` 或 `false` 都报错 `16-level shading switch requires precise rendering`，非布尔值报错 `Invalid quantizeShading`。

`hatchMode` 保留为仅适用于排线样式的兼容别名（layered 对应 true，continuous 对应 false），不影响规则网点；排线中两项同时显式提供且不一致时报错 `Conflicting quantizeShading and hatchMode`。快速模式默认排线仍为 continuous，显式 fast＋layered 仍报错 `Layered hatching requires precise rendering`。后文性能数字、浏览器效果审批及默认切换验证均为此前阶段的历史记录，不是本次共享开关的新测量或完整验证结论。

默认外观有意改变，不承诺与旧默认逐字节或逐像素一致。旧版兼容验证现在应显式使用 continuous 对照提升前的独立基线；以下实验阶段的测量与272组默认一致性记录保留为历史，不代表提升后的默认仍与旧默认一致。

## 实际流程

1. 沿用科学坐标、半径、比例、正交投影及解析可见区域；不重排分子，不缩小原子。
2. 球面排线仍是倾斜平面与球相交的曲线；圆柱仍用母线。解析取朝向相机的骨架，不沿线扫描明暗。
3. 同一半径的球在一次渲染中共用一套局部骨架，通过 `<use transform="translate(...)"/>` 放到不同原子的位置。默认交叉排线下，每个半径最多3个路径定义；C60的球面排线只需3个骨架定义，而非每个碳原子重复生成。
4. 沿用原亮度／对比度映射和排线族出现阈值，将线宽使用的暗度量化为16级；使用 `directionalTonePath()` 解析求阈值区域。
5. 相邻累计区域作 evenodd 差集，得到互不重叠的明暗带。每带引用同一骨架，采用该档固定线宽。`variableWidth: false` 时保持等宽。
6. 整条描边（包括宽度和圆头）由已认证的可见表面路径裁剪，不只检查中心线。
7. 开启阴影时，每个潜在受影表面只发现一次二值阴影区域。正常明暗带裁剪到非阴影部分，阴影明暗带裁剪到阴影部分；两者不叠画。

Hatch与halftone共用 `src/tone-levels.ts` 中的16级约定及解析阈值区域求解器，但分级变量不同：halftone分墨覆盖率，hatch分用于线宽的暗度。16级不是新的物理规律或几何容差。

## 为什么不是照搬累计叠画

第一版沿用halftone的累计区域直接叠画线条。实际Edge截图显示：重复描画细线的抗锯齿边缘会累积变黑，暗部明显过重。因此最终版本改为互斥明暗带，同一位置只使用一个线宽档位。阴影／非阴影也互斥，避免再叠一次描边。

不通过任意调小线宽或改亮度来掩盖这个问题。第一版源码和测量留在 `build/layered-hatching/` 作为实验记录，不用于最终性能结论。

## 视觉与范围

- 保留曲面走向、排线族与交叉排线；不是把球面换成平铺直线。
- 线宽分档，裁断处不再沿线收尖；不是原连续算法的逐像素替代。边界处可能有细微分档／抗锯齿差异。
- 已在Edge查看9组左右对照，覆盖球、乙醇、C60、带标签葡萄糖、DHPC、阴影、常宽、单排线族和高对比度；互斥版本消除了第一版明显叠黑的问题。
- 局部阴影发现网格、原有柱体轮廓及不支持几何的回退仍存在。本实验不宣称整个渲染器已全解析化。
- 可见表面无法认证时回到连续路线，并在SVG根节点标记 `data-hatch-mode="continuous-fallback" data-hatch-fallback="uncertified-visible-regions"`；不会把回退算成实验提速。
- 不保证SVG总是更小：例如样张中的DHPC在scale=28时，分级版约255万字符，连续版约155万；16个裁剪带本身也有成本。常宽单球样张也更大。

## 对照与复现

```sh
npm run build
node scripts/demo-layered-hatching.cjs
node scripts/bench-layered-hatching.cjs
# 原生QuickJS-ng：qjs --std --script scripts/bench-layered-hatching.cjs
node scripts/check-layered-default-parity.cjs
npm test
npm run typecheck
```

`build/layered-hatching/comparison.html` 含9组对照，旁边保存18个独立SVG及浏览器截图。旧版兼容对照脚本沿用 `check-layered-default-parity.cjs` 文件名，以显式 continuous 对照实验之前保存的独立基线 `build/layered-hatching/baseline/`，缺失时失败，不静默跳过；不将新默认与旧默认的差异判为兼容失败。

性能协议：同一进程、每种模式2次预热、5轮交替计时，取中位数。计时只包含SVG生成；确定性检查和实验路线检查在计时之外。V8与原生QuickJS依次运行，不与测试／截图争抢CPU。**不代表浏览器实际绘制速度，也没有声称两种样式逐字节相同。**

## 提升为默认之前的本机测量（历史记录）

Node 26.8.2 / V8与原生QuickJS-ng，preview、800×640、scale=50、默认变宽排线；葡萄糖开启标签，最后一行开启阴影。原始5次样本、参数、生成字符数与构建SHA256见 `benchmarks/layered-hatching.json`。

| 场景 | V8 连续→分级（ms） | 倍率 | QuickJS 连续→分级（ms） | 倍率 |
|---|---:|---:|---:|---:|
| 单球 | 5.43 → 1.73 | 3.13× | 42.22 → 6.21 | 6.80× |
| 乙醇 | 21.68 → 9.52 | 2.28× | 250.21 → 63.50 | 3.94× |
| C60 | 149.80 → 67.63 | 2.21× | 2441.04 → 1052.66 | 2.32× |
| 葡萄糖 | 48.28 → 24.42 | 1.98× | 799.35 → 246.25 | 3.25× |
| DHPC | 137.30 → 69.06 | 1.99× | 2659.14 → 1301.87 | 2.04× |
| 乙醇＋阴影 | 32.04 → 10.36 | 3.09× | 375.23 → 112.75 | 3.33× |

这些只是本机所列配置的结果，不保证任意模型、缩放、常宽模式或浏览器绘制有相同收益。常宽排线本来就较省，不能据此推定同样加速。样张DHPC使用scale=28，与计时的scale=50不同。

## 提升为默认之前已验证的契约（历史记录）

以下为可选实验阶段的验证结果，不是本次默认切换后重新运行的结果。

- 可选实验加入前后的旧默认连续路线：272组SVG严格逐字节相同，包含17个预设、两种渲染模式、两档质量、三种光影，以及阴影／标签／元素纹理组合；快速模式ID也没有改变。
- 实验专用回归：17个预设均走实验路线；500次独立认证可见路径核对、12,943次明暗分带／gamma／阴影反算探针通过。
- 测试验证同一点最多属于一个明暗带、全阴影时常量暗度只使用一个档位、阴影与非阴影区域互补、同半径球复用骨架而不平移世界坐标裁剪区域。
- 另覆盖宽描边与圆头的完整裁剪、参数极值、旋转、无墨输出、输入坐标不变，以及明确的连续回退。
- 新类型和UI预览／导出／模式切换／重置行为的专项检查通过。
- 最终完整 `npm test` 的51个CJS脚本全部通过；另补跑3个MJS脚本（快速渲染、实验绘制顺序、72组白色键组合）通过；严格类型检查与 `git diff --check` 通过。

## 默认提升后的验证（共享开关之前的历史记录）

- 新默认的9组SVG与之前实际浏览器审阅过、独立保存的分级样张逐字节相同，没有借默认切换修改已批准的效果。
- 原生QuickJS验证5个模型：省略或显式undefined均等于精确模式的layered；快速默认等于显式continuous；显式fast＋layered仍明确拒绝。
- 默认勾选、重置恢复开启、取消后的连续预览／导出，以及切换样式／快速模式时保留两种勾选偏好，均通过专项UI测试。
- 默认切换已收尾：51个CJS脚本全部覆盖并通过。完整运行先通过45个，在类型测试的旧填色标签匹配处停止；只修正该测试的标签选择，保留半径／投影容差和模块一致性断言，再补跑最后6个，全部通过。未重复前45个，也不将这次记录描述成单次 `npm test` 无中断通过。
- 3个补充MJS测试、272组显式连续兼容对照、9组已批准样张对照、严格类型检查和差异检查均通过。渲染算法未在收尾阶段改动。
- 本次日志：`build/layered-hatching/closeout-targeted.log`、`closeout-full.log`、`closeout-remainder.log`。
- 在该次默认提升收尾时，halftone／hatching共用16级开关及点刻优化仍属于后续工作，当时没有实施；当前共享开关契约见本文开头，不改写该历史验证范围。
