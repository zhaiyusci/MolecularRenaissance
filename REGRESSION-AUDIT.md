# 九项回归失败调查

> 本文保留修复前的调查记录。随后已实施有条件的相切支持并同步测试契约；当前修复与验证状态见 `REGRESSION-FIXES.md`，不要把下述历史失败当作当前状态。

## 结论

**不是九处独立的渲染算法错误，但也不能全部当成过期测试。**

- 七个脚本的当前首个失败主要来自接口、投影基线、SVG表示或内部调用路径的测试失配。
- 两个脚本直接暴露同一个真实覆盖缺口：**正常预设中相切/近相切的原子球导致整场解析边界回退**。
- 进一步验证发现明确的用户可见影响：**过氧化氢、异丙醇、甘氨酸、葡萄糖、磷脂这五个预设，在精确模式开启标签时，全部标签被省略。** 快速模式有标签。该问题在`b351c7b`和优化后的版本均存在。

本次只调查：**没有修改生产代码、现有测试或历史基线，没有重新构建或commit/push**。诊断使用内存中的受控插桩和反事实对照，不把修改过的诊断当作原测试通过。

## 逐项分类

| 测试脚本 | 已验证根因 | 分类及处理 |
|---|---|---|
| `test-analytic-boundaries.cjs` | 五个预设在外切球保护条件提前返回null；其他共享轮廓、禁止网格回退、浏览器断言受同一失败影响 | **真实解析支持缺口**。不能只取消“所有预设解析成功”的要求而忽略标签缺失 |
| `test-hatch-intervals.cjs` | 同五个场景整场回退，未进入它们的逐曲线检查；并非新出现的某条排线区间错误 | 同一个真实覆盖缺口，不是第二个独立算法缺陷 |
| `test-hatch-renderer.cjs` | 测试给legacy clipCircle/clipLine打钩子，但当前surface atlas路径不调用它们，所谓“第一次返回null”从未注入 | **路由测试过期**；明确强制被测分支，再测试单曲线回退隔离 |
| `test-hatch-optimization.cjs` | 用旧球心(450,338)的积分窗口测新球心(450,350)，把空白也算进去了；后面的点纹历史比对还有同类投影问题 | **基线坐标过期**，并非排线墨量近乎减半；保留原墨量容差、改用相匹配的投影 |
| `test-shared-regions.cjs` | 历史fixture采用旧投影；随机点纹绑定绝对屏幕网格，分子移动后点位置/数量自然改变 | **历史投影过期**；只对齐历史投影后，每一个点精确一致 |
| `test-c60.cjs` | 仅以出现`<circle`判定点纹存在，但实际已经是圆端帽零长度path批量编码 | **SVG表示断言过期**；实际158,830个点仍存在，不能以标签拼写判断有没有绘制 |
| `test-dot-regions.cjs` | 旧参数布局、已移除颜色回调、旧的单抖动点查询假设与当前接口/Poisson单元查询不同，不只是8参数变9参数 | **多层测试失配**；修正接口后仍需正视五个预设没有认证区域的问题 |
| `test-typescript.cjs` | 导出名单仍要求已删除的elementInkColor/elementInkPalette，遗漏新增API；后续typed consumer还传已删除colorMode | **契约fixture未同步**；当前三种模块格式一致，移除过时选项后严格类型消费者通过 |
| `test-label-depth.cjs` | 核心标签遮挡检查已通过，最后可选历史SVG比较仍用旧投影 | 此断言是**投影基线过期**；它没有覆盖前述五个回退预设的真实标签缺失 |

“首个失败过期”不等于整个脚本只需改一行，也不等于应用不存在其他问题。

## 一、应优先修复：相切场景的标签丢失

### 触发条件与数值证据

`src/boundaries.ts`（124–143行）先排除明确分离的球，再对以下情况直接返回null：

```ts
if (d >= sum - 1e-8 || d <= Math.abs(a.r-b.r) + 1e-8) return null;
```

本次五个失败均命中**外切/近外切**一项，而不是内含球、偶遇杆接触或摄影机投影问题。独立用场景坐标和半径计算，各场景遇到的第一对如下（原子索引从0起）：

| 预设 | 球对 | 中心距Å | 半径和Å |
|---|---|---:|---:|
| 过氧化氢 | H2 / O0 | 0.9700000000000000 | 0.97 |
| 异丙醇 | C2 / C0 | 1.5199999999999998 | 1.52 |
| 甘氨酸 | C1 / C0 | 1.5200000000000000 | 1.52 |
| 葡萄糖 | C1 / C0 | 1.5199999991467108 | 1.52 |
| 磷脂 | C13 / C12 | 1.5199999979292762 | 1.52 |

所有这些球对都有化学键连接。共价半径H=.31、C=.76、N=.71、O=.66Å，与预设中的常见键长自然构成此类接触。因此不能通过改小半径、挪动坐标来绕过问题。

### 为什么画面还有，但标签全没了

- `src/renderer.ts`（54行）只有认证的`surfacePaths`才能创建分层表面。
- 同文件160–162行明确规定：无法认证所有权时省略标签，避免文字错误地浮在前景原子上面。
- 主体几何仍有数值回退，所以“不崩溃、SVG有限”掩盖了功能丢失。
- 元素纹理另有采样所有权蒙版（`src/element-textures.ts`75–124行），不等于认证的标签绘制层。

新做了48次渲染：五个失败预设及乙醇对照 × 原版/新版 × 精确/快速 × 元素纹理开/关。参数为300×240、preview、labels:true，并关闭排线及轮廓以隔离标签行为。

| 预设 | 精确模式标签节点 | 快速模式标签节点 |
|---|---:|---:|
| 过氧化氢 | 0 | 4 |
| 异丙醇 | 0 | 12 |
| 甘氨酸 | 0 | 10 |
| 葡萄糖 | 0 | 24 |
| 磷脂 | 0 | 70 |
| 乙醇对照 | 7 | 9 |

这些是输出的text节点数量，不是声称快速模式的所有文字最终都无遮挡。原版与新版结果相同，元素纹理开关不改变漏标签结论。48个SVG均有限；精确纹理开启时仍输出采样蒙版，但本次未对这些近切场景的全部纹理边界做独立认证。

### 修复方向

优先建立安全的相切支持或认证标签回退，**不要直接删除epsilon保护，也不要把所有标签最后覆盖到画面上**。

一个值得验证的方向是：这些接触对都有半径.115Å的连接杆，接触点/微小交线可能被杆完整遮蔽；若能证明径向、轴向覆盖及误差范围，可以安全省略不可见的退化接缝。这里仅提出可验证方向，尚未实现或证明。

回归需覆盖这五个预设、相切两侧扰动、多视角、遮挡/标签层顺序，以及元素纹理开关。当前临时可选快速模式，但不能把它当作精确模式问题的正式修复。

## 二、排线/点纹历史基线：反事实验证

当前投影在`src/scene.ts`62–65行以原子质心为旋转中心，并映射到精确画布中心。旧fixture使用包围盒中心和额外的-12像素竖直偏移。

### 排线墨量并未接近减半

对相同单球做实际联合墨迹覆盖积分（投影分别匹配）：

| 测量 | 墨量比例 |
|---|---:|
| 旧球，旧正确窗口 | 0.19063173 |
| 新球，误用旧窗口 | 0.10263396 |
| 新球，新正确窗口 | 0.19097922 |

正确比较差值为0.00034749，即约0.03475个百分点，小于测试原有2个百分点阈值。没有放宽阈值；测试采样区域需要随明确的投影契约更新。当前SVG大小也独立满足后续体积断言。

### 点纹精确一致性

仅在内存中替换旧fixture的投影表达式，不更改旧点纹算法或磁盘文件：

- 单球：5,838个点；乙醇：17,866个点；C60：158,830个点。
- 对`pre-regions.mjs`、`pre-shading.mjs`，对齐投影后这些点的序列化位置、半径、多重集合均与当前**逐点精确相同**。
- 全部5,838个单球点的完整圆形均在球轮廓内，最小余量约0.00527 SVG单位。
- `pre-labels.mjs`只改投影后，单球/乙醇/C60 × 三种光影的9个**完整SVG**也均与当前逐字节相同。

屏幕空间随机数种子绑定单元索引，移动分子会改变取样，不能只是把已有点坐标减去一个平移量然后要求相等。

`test-shared-regions.cjs`的disks辅助函数只读取第一个dots组，是另一个潜在测试缺陷，但不是此次默认参数失败原因：这几个默认测试未开启标签/元素纹理，输出仅一个dots组。将来覆盖多owner图层时必须解析全部实际绘制组。

## 三、C60不是丢了点

`src/dots.ts`208–218行用`stroke-linecap="round"`的`M x y h0`子路径表示圆盘，线宽为直径；测试仍只找circle标签。

独立检查：

- C60点纹为158,830个真实圆盘，preview/export均有实际非零墨量。
- 取1,024个分布于输出中的点，用物理depthAt独立查询中心所有权，再做16,384个圆周所有权探测：未发现丢失中心或越界所有权。
- 半调为11个pattern定义、616个实际色调绘制矩形，不应把defs里的圆当作已绘制的点。
- 六组光影/质量输出均有限；后续旋转半调检查也通过。

修复测试时应识别实际绘制的几何语义和覆盖率，保留拓扑、有限性、所有权及墨量检查。现有`<path`/`<circle`字符串判断既会误报，也可能仅因为defs存在而漏报空画面。

## 四、接口与内部路线检查

### 点区域ABI

当前`buildDots`为：

```text
(scene, depthAt, project, scale, illumination,
 options, regions, referenceCoverage, surfaces)
```

`test-dot-regions.cjs`仍按旧的颜色回调参数解包，并调用已删除的`elementInkColor`；旧的单抖动点查询检查也不适用于现在的Poisson单元认证。只把8改成9并不足以修好测试。

已用当前ABI捕获17个实际场景：12个有认证区域，5个为上述相切回退。对有认证区域的12个场景复用原测试独立物理深度/边界余量oracle，保留原90×78网格、圆周探测和容差：12个场景全部通过，共84,240个网格点、9,482,496个圆周探测；按原规则保留14个独立确认的边界带例外，没有额外缩小安全余量。结果见`build/failure-audit/dot-region-geometry.json`。这不是宣称整个旧脚本已通过。

### 排线fallback测试没有注入到实际路径

默认surface atlas优先处理裁剪，旧`clipCircle`/`clipLine`钩子调用次数是0/0。仅在诊断内存中关闭atlas后，调用数为339/46；把第一条圆曲线裁剪返回null后，后续计数仍为339/46，SVG有限，说明未观察到“一个fallback使其他解析裁剪失效”。应更新测试路由而非强迫生产代码走旧接口。

### 类型与模块一致性

CommonJS、ESM和无DOM经典脚本的16个导出完全一致；三种光影的苯酚输出也逐字节一致。`PALETTES.md`63行已明确记录移除colorMode、elementInkColor/Palette及颜色回调。

独立严格编译消费者，仅报旧fixture第5行使用colorMode；在内存中只移除该过时选项后，类型诊断为零，其他负面类型断言保留。修复应同步明确的API名单/消费者契约，并测试被移除选项确实不再接受，而不是放弃严格类型检查。

## 测试设施本身的问题

历史fixture位于git忽略的`build/baselines/`，多个测试只有文件存在才运行旧基线断言，导致同一个提交在不同工作目录可能执行不同检查。应改为受版本控制、带投影/参数元信息的固定fixture，或从固定提交可重复生成；不能以文件缺失时跳过作为通过依据。

建议顺序：

1. **P1：修复相切预设的精确模式标签缺失，并补用户功能回归。**
2. **P1：修复测试ABI/语义解析与可重复fixture，恢复可信的质量门禁。**
3. P2：将认证路径、合法回退路径分开测试，测量回退成本；不把已知正常预设静默降级当作理所当然。

本报告不是全仓库所有测试的修复或全面正确性证明。没有进行浏览器截图验证；几何与SVG语义验证按上述范围进行。此前性能优化的前后逐字节一致只能证明没有新增这些差异，不能替代这次缺陷调查。

## 原始证据和复现

诊断脚本/结果均在`build/failure-audit/`，不修改生产源码：

```powershell
node build/failure-audit/analytic-timeout.cjs
node build/failure-audit/analytic-timeout.cjs analytic-impact.cjs
node build/failure-audit/mark-probes.cjs
node build/failure-audit/c60-footprints.cjs
node build/failure-audit/api-label-diagnostics.cjs
node build/failure-audit/dot-region-geometry.cjs
```

详细分组报告：`analytic-findings.md`、`mark-findings.md`；父诊断JSON：`api-label-results.json`、`dot-region-geometry.json`。这些诊断依赖现有构建、`build/optimization-baseline-repo/`快照以及历史fixture；完整前后测试环境准备见`benchmarks/HOTPATH-OPTIMIZATION.md`。
